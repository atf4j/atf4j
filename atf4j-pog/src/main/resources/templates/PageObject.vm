## PageObject.vm
// generated from main/resources/templates/PageObject.vm
// $timeStamp

package $packageName;

import static org.junit.Assert.*;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

import net.atf4j.webdriver.page.AbstractPageObject;

public class ${className} extends AbstractPageObject {

#foreach($element in $pageElements)
	${element}
#end

	public $className() {
		super();
	}

	public ${className}(WebDriver webDriver) {
		super(webDriver);
	}

	public ${className} open() {
		super.open("${pageUrl}");
		return this;
	}

	public ${className} verify() {
		super.verifyPageTitle("${pageTitle}");
		return this;
	}

	// Navigation Elements.
#foreach($element in $navigation)
	// ${element.fieldName}
	public ${className} navTo$util.firstUpper(${element.fieldName})() {
		assertNotNull($element.fieldName);
		${element.fieldName}.click();
		return this;
	}
#end

	// Input Elements
#foreach($element in $inputs)
	// ${element.fieldName}
	public ${className} inputTo${element.fieldName}(String inputText) {
		assertNotNull(${element.fieldName});
		${element.fieldName}.sendKeys(inputText);
		return this;
	}
#end

	// Content Elements
#foreach($element in $content)
	// ${element.fieldName}
	public ${className} verify${element.fieldName}(String inputText) {
		assertNotNull(${element.fieldName});
		verify(${element.fieldName});
		return this;
	}
#end
}
