## PojoClass.vm
// from main/resources/PojoClass.vm
package $packageName;

public class $className {

#foreach($field in $class.Fields)
	private $field.Type $field.Name;
#end

#foreach($field in $class.Fields)
	public $class.Name set$utility.methodCase($field.Name)(final $field.Type $field.Name) {
		this.$field.Name = $field.Name;
	}
	public $field.Type get$utility.methodCase($field.Name)() {
    	return this.$field.Name;
 	}
#end
}
